#include<iostream>
#include<vector>
#include<queue>
#include<unordered_map>
#include<string>
using  std::string, std::cout, std::cin, std::endl;

unordered_map<int, vector<int>> adj_lst;

void search(curr_node, vector<bool>& visited, max){
  visited(max, False);
  list<int> q;

  visited[curr_node] = true;
  q.push_back(curr_node);

  while(!q.empty()){
    int s = q.first();
    q.pop();
     	
  }
}
  
